---
import { t, altLocale, localePath, type Locale } from '../i18n';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const other = altLocale(locale);
---

<a
  href={localePath(other)}
  class="lang-switch"
  aria-label={t(locale, 'lang.switch_label')}
>{t(locale, 'lang.switch')}</a>

<style>
  .lang-switch {
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    padding: 0.2rem 0.5rem;
    border: 1px solid var(--c-bordure);
    border-radius: var(--r-petit);
    color: var(--c-texte-secondaire);
    text-decoration: none;
    transition: color 0.2s, border-color 0.2s;
  }
  .lang-switch:hover {
    color: var(--c-texte);
    border-color: var(--c-texte-secondaire);
  }
</style>
